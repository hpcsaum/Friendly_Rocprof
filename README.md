# Friendly Rocprof

Bash scripts and post-processing tools for working with `rocprof` output.

## Layout

- `scripts/` — bash scripts for running/wrapping rocprof
- `postprocess/` — tools for parsing and analyzing rocprof output
